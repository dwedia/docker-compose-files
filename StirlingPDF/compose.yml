version: '3.3'
services:
  stirling-pdf:
    image: docker.stirlingpdf.com/stirlingtools/stirling-pdf:latest
    ports:
      - '8080:8080'
    volumes:
      - /opt/persistentStorage/StirlingPDF/trainingData:/usr/share/tessdata # Required for extra OCR languages
      - /opt/persistentStorage/StirlingPDF/extraConfigs:/configs
      - /opt/persistentStorage/StirlingPDF/customFiles:/customFiles/
      - /opt/persistentStorage/StirlingPDF/logs:/logs/
      - /opt/persistentStorage/StirlingPDF/pipeline:/pipeline/
    environment:
      - DISABLE_ADDITIONAL_FEATURES=false
      - LANGS=en_GB
